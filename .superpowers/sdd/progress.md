# Client-Data Layout Isolation - SDD progress ledger

Plan: docs/superpowers/plans/2026-07-22-client-data-layout.md

Task 1: complete (commits c5db2bc..3c36ab9, fix 3c36ab9, re-review clean - Spec ✅, Approved. Real Important finding caught: Remove-LegacyClientFiles's client-version.txt deletion lacked the exe's path-equality guard, would self-delete a freshly-written version marker if -InstallPath explicitly targeted the legacy bare root, causing a reinstall loop - fixed, also corrected in the plan doc itself for Task 2's identical copy)
Task 2: complete (commits 3c36ab9..0456b05, review clean - Spec ✅, Approved. Confirmed the version-marker deletion guard from Task 1's fix carried over correctly into this file's own Remove-LegacyClientFiles copy. 1 Minor cosmetic note: report's stated line count off by a few, not a code issue)
Task 3: complete (commits 0456b05..47ac867, review clean - Spec ✅, Approved, no findings. All 3 shared-root cases traced correct. 1 Minor note not fixed: pre-existing "Client removed." Write-Host still prints even when the guard skips removal - cosmetic, unchanged pre-existing line, flag for final review)
Task 4: complete (commits 47ac867..f4715f7, review clean - Spec ✅, Approved, no findings. Extraction into $script:RemoveClientScriptBlock confirmed behavior-preserving; inlined guard uses target machine's own $env:ProgramData correctly; all 3 cases traced correct. 1 Minor plan-mandated note: test coverage misses the "shared root but no server-config.json" branch specifically, not fixed - low risk)
Task 5: complete (commits f4715f7..f57acec, review clean - Spec ✅, Approved, no findings. Version bump verified correct - only server ProductVersion 0.21.3->0.22.0, client untouched. Both READMEs + CHANGELOG accurate against shipped Tasks 1-4 behavior. 2 Minor cosmetic notes both plan-mandated (brief's own text), not implementer issues)
Final whole-plan review: complete (opus, range c5db2bc..f57acec) - Ready to merge: Yes. No Critical/Important findings. Independently re-verified the Task 1/2 client-version.txt guard fix correct by tracing both copies side by side. Confirmed the migrate-then-uninstall interaction scenario (Deploy-ClientGpo migrates a client to client-data, then Uninstall-ClientWinRM runs against it) works correctly - the guard keys off the OLD/shared root, correctly does not fire post-migration. 3 Minor findings: (1) fixed directly in commit 8f938dd - Uninstall-Client.ps1's unconditional "Client removed." contradicted the guard's skip-warning, moved inside the branch where removal actually happens; (2) accepted, not fixed - no test for WinRM guard's "shared root but server-config.json absent" branch specifically (low risk, code identical to the already-tested Uninstall-Client copy); (3) accepted, not fixed - one WinRM test doesn't redirect $env:ProgramData, touches real C:\ProgramData\WindowsLicenseInventory (harmless in practice, that legacy path never exists on a modern dev machine, but worth tightening if this test file is touched again).
